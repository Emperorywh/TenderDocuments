# evaluation

AI 抽取与解析质量的评测目录。

## 职责

依据 `SPEC.md` 第 9 章和第 14.2 节，记录 DeepSeek 与解析链的召回率、证据正确率等指标。AI 指标仅记录、暂不阻断发布。

## 边界

- 未经授权和脱敏的数据不得进入公共评测集（`SPEC.md` 第 6.16 节）；
- 评测样本与验收样本集中在 `TASKS.md` 阶段 L 准备，不与本目录内的离线评测脚本混放。

具体内容由后续评测任务（见 `TASKS.md`）按依赖逐步补充。
